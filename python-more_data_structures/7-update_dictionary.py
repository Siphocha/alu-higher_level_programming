#!/usr/bin/python3
# This script has a function that replaces/adds key value pairs to a dictionary.

def update_dictionary(a_dictionary, key, value):
    a_dictionary[key] = value
    return (a_dictionary)